[Unit]
Description=Auto-Deploy Trigger
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/bin/curl \
  -H 'Content-Type: application/json' \
  -d '{"type": "{{ ansible_event_type | default('manage') }}"}' \
  127.0.0.1:33337/endpoint

[Install]
WantedBy=multi-user.target
